export enum SUPPORTED_LANGUAGES {
  RU = 'ru'
}

export const supportedLngs: SUPPORTED_LANGUAGES[] = Object.values(SUPPORTED_LANGUAGES);

export const defaultLang = 'ru';
export const defaultNS = 'common';